## 1.0.0

- Initial release
- `Stubkit.configure()` and `Stubkit.shared` singleton
- `isActive()` entitlement check
- `getEntitlements()` list all user entitlements
- `syncPurchase()` sync IAP receipts
- `refresh()` force-refresh entitlements
- Automatic retry with exponential backoff on 5xx errors
